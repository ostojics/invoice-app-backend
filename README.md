# Setup

```
yarn install
docker compose up -d
yarn db:setup
yarn start
```

## Running Tests

To run tests and see the coverage, run the following command

```bash
  yarn test:cov
```

## Tech Stack

NestJS, Prisma, Docker
